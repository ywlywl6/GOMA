# 04_attn_context.yaml
# 说明: Score * V 计算最终上下文
{{include_text('../../matrix_base.yaml')}}
problem:
  <<<: *matrix_base
  instance:
    # GQA 映射: X = SeqLen(32768) * (Hq/Hkv)(4) = 131072
    X: 131072
    Y: 64      # Head Dim (输出维度)
    Z: 32768   # SeqLen (归约维度)
