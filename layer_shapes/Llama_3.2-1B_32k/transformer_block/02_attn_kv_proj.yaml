# 02_attn_kv_proj.yaml
# 说明: 生成 Key 和 Value 矩阵。
# 注意: 因为有 K 和 V 两个矩阵，计算总耗时/能耗时，该 Workload 需要执行 2 次。
{{include_text('../../matrix_base.yaml')}}
problem:
  <<<: *matrix_base
  instance:
    X: 32768
    Z: 2048    # Input (Hidden Size)
    Y: 512     # Output (KV_Heads 8 * Dim 64) - GQA 节省了计算量
