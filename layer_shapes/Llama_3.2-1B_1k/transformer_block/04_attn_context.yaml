# 04_attn_context.yaml
# 说明: Score * V 计算最终上下文
{{include_text('../../matrix_base.yaml')}}
problem:
  <<<: *matrix_base
  instance:
    # GQA 映射: X = SeqLen(1024) * (Hq/Hkv)(4) = 4096
    X: 4096
    Y: 64      # Head Dim (输出维度)
    Z: 1024    # SeqLen (归约维度)
