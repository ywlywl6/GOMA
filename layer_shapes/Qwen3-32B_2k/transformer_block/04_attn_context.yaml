# 04_attn_context.yaml
# 说明: Score * V
{{include_text('../../matrix_base.yaml')}}
problem:
  <<<: *matrix_base
  instance:
    # GQA 映射: X = SeqLen(2048) * Ratio(8) = 16384
    X: 16384
    Y: 128     # Head Dim (输出维度)
    Z: 2048    # SeqLen (归约维度 - Updated)
