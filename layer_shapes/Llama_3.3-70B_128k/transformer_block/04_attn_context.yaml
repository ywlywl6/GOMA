# 04_attn_context.yaml
# Score * V
# GQA Flattened Mapping 应用于此处
{{include_text('../../matrix_base.yaml')}}
problem:
  <<<: *matrix_base
  instance:
    # GQA Stacked X: SeqLen(131072) * (Hq/Hkv)(8) = 1048576
    X: 1048576
    Y: 128     # Head Dim (输出维度)
    Z: 131072  # Value SeqLen (归约维度)
