# 04_attn_context.yaml
# 说明: Score * V
{{include_text('../../matrix_base.yaml')}}
problem:
  <<<: *matrix_base
  instance:
    # GQA 映射: X = SeqLen(1024) * Ratio(2) = 2048
    X: 2048
    Y: 128     # Head Dim (输出维度)
    Z: 1024    # SeqLen (归约维度)
